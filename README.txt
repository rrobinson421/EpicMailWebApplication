How to run the project:
Navigate to project directory

Type these commands:
npm install
npm run dev

Then, on a different terminal, type these commands from the project directory:
cd server
pip install -r requirements.txt
python main.py

Navigate to http://localhost:5173/ in browser